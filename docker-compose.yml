services:
  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - kev_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: kev_catalog

volumes:
  kev_mongo_data:
